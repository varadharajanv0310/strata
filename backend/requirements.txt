# strata backend — CORE deps (API + warehouse + ingestion).
# Installs without the multi-GB CUDA stack so the API/warehouse run immediately.
# For the GPU pipeline, also install requirements-ml.txt (see BUILD_LOG D2).

# ---- API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20          # resume upload (multipart/form-data)

# ---- databases ----
SQLAlchemy==2.0.36
alembic==1.14.0
duckdb==1.1.3

# ---- data IO ----
pyarrow==18.1.0                   # parquet
pandas==2.2.3
numpy==2.2.1
requests==2.32.3
httpx==0.28.1
warcio==1.7.5                     # Common Crawl WARC parsing
beautifulsoup4==4.12.3           # JSON-LD / HTML extraction fallback

# ---- auth ----
bcrypt==4.2.1
PyJWT==2.10.1

# ---- resume parsing (CPU) ----
pdfminer.six==20240706
python-docx==1.1.2

# ---- orchestration ----
prefect==3.1.11

# ---- validation / quality ----
great-expectations==1.3.1        # optional; pipeline has a lightweight built-in fallback if absent

# ---- dev / test ----
pytest==8.3.4
pytest-asyncio==0.25.0
