# strata backend — CORE deps (API + warehouse + ingestion), pinned to tested versions.
# Installs without the multi-GB CUDA stack so the API/warehouse run immediately.
#   GPU pipeline   → backend/requirements-ml.txt
#   orchestration/validation (optional, import-guarded) → backend/requirements-optional.txt

# ---- API ----
fastapi==0.116.2
uvicorn[standard]==0.36.0
pydantic==2.11.9
pydantic-settings==2.14.1
python-multipart==0.0.26          # résumé upload (multipart/form-data)
email-validator==2.3.0            # pydantic EmailStr (accounts)

# ---- databases ----
SQLAlchemy==2.0.43
alembic==1.18.4
duckdb==1.5.3

# ---- data IO ----
pyarrow==21.0.0                   # parquet
pandas==2.3.2
numpy==2.2.6
requests==2.32.5
httpx==0.28.1
warcio==1.8.1                     # Common Crawl WARC parsing
beautifulsoup4==4.13.5            # JSON-LD / HTML extraction

# ---- auth ----
bcrypt==5.0.0
PyJWT==2.10.1

# ---- résumé parsing (CPU) ----
pdfminer.six==20260107
python-docx==1.2.0

# ---- dev / test ----
pytest==9.0.3
pytest-asyncio==1.4.0
